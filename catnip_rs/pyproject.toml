# Standalone build config (for Rust-only development/debugging)
# Production build uses setuptools-rust via root setup.py

[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "catnip-rs"
version = "0.0.6"
requires-python = ">=3.12"

